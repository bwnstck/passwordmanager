import React from "react";

import { Wrapper as WrapperEle } from "./Wrapper";

export default {
  title: "-=PWSafe2000=-/Body",
  component: WrapperEle,
};

const Template = (args) => <WrapperEle {...args} />;

export const Wrapper = Template.bind({});
