import React from "react";

import { Button as ButtonElement } from "./Button";

export default {
  title: "-=PWSafe2000=-/Body",
  component: ButtonElement,
};

const Template = (args) => <ButtonElement {...args} />;
export const Button = Template.bind({});
