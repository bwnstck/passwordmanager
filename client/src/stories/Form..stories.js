import React from "react";

import FormElement from "./Form";

export default {
  title: "-=PWSafe2000=-/Body",
  component: FormElement,
};

const Template = (args) => <FormElement {...args} />;

export const Form = Template.bind({});
